import React, { useState } from 'react';
import styled from 'styled-components';

const WorkCard = styled.section `

`; 

const WorkCardInner = styled.div`

`;

const Portfolio = () => {

    return (
            <p>Portfolio</p>
    );
}

export default Portfolio;